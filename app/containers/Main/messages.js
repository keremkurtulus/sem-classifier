/*
 * Main Messages
 *
 * This contains all the text for the Main container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Main';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'SEM Classifier!',
  },
});
